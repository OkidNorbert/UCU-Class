CREATE TABLE programs(PID CHAR(4) PRIMARY KEY, PNAME VARCHAR(50) NOT NULL);
CREATE TABLE students(S_ID CHAR(4) PRIMARY KEY, SNAME VARCHAR(50) NOT NULL, Gender CHAR(1) CHECK (Gender IN ('M', 'F')), PID CHAR(4), FOREIGN KEY (PID) REFERENCES programs(PID));
desc programs;
desc students;
INSERT INTO students (S_ID, SNAME, Gender, PID) VALUES  ('SOO1'. 'SIMON', 'M', 'P003'), ('S002', 'MARY', 'F', 'P001'), ('S003', 'JOHN', 'M', 'P002');
ALTER TABLE students ADD DOB DATE;
ALTER TABLE students RENAME COLUMN S_ID TO Student_ID;
SELECT * FROM students WHERE Gender = 'M';

